version: 2
jobs:
  puppetlabs_spec_ruby32:
    docker:
      - image: cimg/ruby:3.2
    steps:
      - checkout
      - run: bundle install
      - run: bundle exec rake

workflows:
  version: 2
  test:
    jobs:
      - puppetlabs_spec_ruby32
